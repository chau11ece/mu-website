<<<<<<< HEAD
name: MU Website CI/CD

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: chaudevops/mu-website:latest
=======
- name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .         # It's now in the root of this specific repo!
          push: true
          tags: chaudevops/mu-website:latest
          
>>>>>>> 92d0a8a (Final web build)
